#include <Math/math.hpp>

struct Particle {
    Math::Vec3 position;
    Math::Vec3 velocity;
    // float lifetime = 0.0f;

    // float color;
    // Math::Quat orientation;
    // Math::Vec3 scale;
    // Geometry geometry;
    // Math::Vec3 acceleration;
};