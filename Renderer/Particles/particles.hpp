struct Particle {
    // Math::Vec3 position;
    // Math::Quat orientation;
    // Math::Vec3 scale;
    // Geometry geometry;

    // Math::Vec3 velocity;
    // Math::Vec3 acceleration;

    float color;
    float lifetime;
};